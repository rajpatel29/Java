package Exception_Handling_Method_Overriding;

public class MainClass 
{
	public static void main(String args[])
	{
		//	If the superclass method does not declare an exception
		//		If the superclass method does not declare an exception, subclass overridden method cannot declare 
		//		the checked exception (1) but it can declare unchecked exception (2).
		
		//	If the superclass method declares an exception
		//		If the superclass method declares an exception, subclass overridden method can declare same,
		//		subclass exception or no exception but cannot declare parent exception (3).
		
		
		
		//	Can we change an exception of a method with throws clause from unchecked to checked while overriding it?
		//	If super class method throws exceptions in sub class if you want to mention throws  then use  same class 
		//	or its  sub class exception.
		//	So we can not change from unchecked to checked 
		
		
		
		
		
	}
}
